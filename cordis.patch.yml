# dsh-font-settings bundle patch
#
# Insert one loader entry for the plugin package.
# The browser half is picked up by dsh-client-modules through the
# package's dsh.client declaration.
- insert:
    - id: font-settings
      name: 'dsh-font-settings'