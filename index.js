'use strict';
/**
 * dsh-font-settings — installed (bundle) HOST half.
 *
 * Pure client-side font settings — nothing to do in the host realm.
 */
const NAME = 'dsh-font-settings';

function apply() {
  // Pure client-side plugin — no host-side work needed.
}

module.exports = {
  name: NAME,
  apply
};